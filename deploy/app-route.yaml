kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: blog-route
  namespace: fiap-aso
spec:
  host: blog-fiap-aso.apps.na46.prod.nextcle.com
  to:
    kind: Service
    name: blog
    weight: 100
  port:
    targetPort: 8080-tcp
  wildcardPolicy: None